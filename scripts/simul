#!/bin/sh
# PATH wrapper for the simul CLI. Build once with: ./gradlew installDist
# Then e.g.:  ln -s "$(pwd)/scripts/simul" ~/.local/bin/simul
exec "$(dirname "$(readlink "$0" || echo "$0")")/../build/install/simul/bin/simul" "$@"
